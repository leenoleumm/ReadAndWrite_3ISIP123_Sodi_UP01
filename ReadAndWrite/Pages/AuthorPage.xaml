<Page x:Class="ReadAndWrite.AuthorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Кабинет автора" Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Кабинет автора" FontSize="28" FontWeight="Bold" Foreground="#1e3a5f"/>

        <Button x:Name="AddBookBtn" Grid.Row="1" Content="+ Добавить новую книгу" 
                Width="220" Height="35" Margin="0,15,0,0" Background="#27ae60" 
                Foreground="White" BorderThickness="0" FontSize="14" Cursor="Hand" 
                Click="AddBook_Click"/>

        <Button x:Name="FrozenBooksBtn" Grid.Row="2" Content="🔒 Замороженные книги" 
                Width="220" Height="35" Margin="0,10,0,0" Background="#e74c3c" 
                Foreground="White" BorderThickness="0" FontSize="14" Cursor="Hand" 
                Click="FrozenBooksBtn_Click"/>

        <TextBlock Grid.Row="3" Text="Мои книги" FontSize="20" FontWeight="Bold" 
                   Margin="0,20,0,10" Foreground="#1e3a5f"/>

        <ScrollViewer Grid.Row="4">
            <WrapPanel x:Name="AuthorBooksPanel"/>
        </ScrollViewer>
    </Grid>
</Page>